#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
    float x1,y1,x2,y2;
    printf("Coordinates of first point:\n");
    scanf("%f %f",&x1 &y1);
    printf("Coordinates of second point:\n");
    scanf("%f %f",&x2 &y2);
    distance = sqrt(pow((x1-x2),2)+pow((y1-2),2));
    printf("Distance between two points:%f\n",celsius);
    return 0;
}
